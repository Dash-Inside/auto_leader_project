import 'package:flutter/material.dart';

/// Section for displaying all possible tarrifs.
class TarrifsView extends StatelessWidget {
  /// Returns an instance of [TarrifsView].
  const TarrifsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
